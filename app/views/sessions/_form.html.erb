<%= form_for(@session) do |f| %>
  <% if @session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bandName %><br>
    <%= f.text_field :bandName %>
  </div>
  <div class="field">
    <%= f.label :sessionDate %><br>
    <%= f.date_select :sessionDate %>
  </div>
  <div class="field">
    <%= f.label :sessionTime %><br>
    <%= f.time_select :sessionTime %>
  </div>
  <div class="field">
    <%= f.label :sessionLength %><br>
    <%= f.number_field :sessionLength %>
  </div>
  <div class="field">
    <%= f.label :comments %><br>
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
